# RockJoint

MATLAB source code lives in `TraceLength/`.

## Python Gradio demo

A minimal Python demo is included to upload an image and return an annotated version.

### Setup (uv)

```bash
uv venv
source .venv/bin/activate
uv pip install -r requirements.txt
```

### Run

```bash
python app.py
```
